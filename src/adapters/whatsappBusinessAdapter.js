export class WhatsAppBusinessAdapter {
  constructor() {
    throw new Error(
      'WhatsApp Business API is a paid production adapter. Add Meta credentials before enabling this provider.',
    );
  }
}
